{
  "dependencies": {
    "netlify-lambda": "^1.6.3"
  },
  "scripts": {
    "postinstall": "netlify-lambda install src/lambda",
    "build": "netlify-lambda build src/lambda -c webpack.config.js"
  }
}
