{
  "dependencies": {
    "encoding": "^0.1.12",
    "netlify-lambda": "^1.6.3"
  },
  "scripts": {
    "build": "netlify-lambda install src/lambda && netlify-lambda build src/lambda -c webpack.config.js"
  }
}
